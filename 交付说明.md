# Mirror 项目交付包

## 包含内容

- `index.html` — 原始 HTML 原型（v0.3，15 屏）
- `lib/` — Flutter 可交互 App（单手机框 + Tab 导航）
- `android/` `ios/` `macos/` `web/` — 各平台工程

## 环境要求

- [Flutter SDK](https://docs.flutter.dev/get-started/install)（建议 3.16+，本项目 `sdk: ^3.11.5`）
- Chrome（Web 预览）或 macOS（桌面预览）

## 快速运行

```bash
# 解压后进入项目目录
cd mirror
flutter pub get
flutter run -d chrome
```

启动后：欢迎页点「邮箱注册」或第三方登录 → 进入主界面，底部可切换 Tab。

## 可选：只看 HTML 原型

用浏览器直接打开 `index.html` 即可，无需安装 Flutter。

## 可选：15 屏画廊

将 `lib/main.dart` 入口改为 `GalleryPage()`（当前默认为可交互 `MirrorPrototype`）。

## 说明

压缩包已排除 `build/`、`.dart_tool/` 等缓存；对方解压后首次 `flutter pub get` 会重新下载依赖。
